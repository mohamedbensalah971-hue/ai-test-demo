pipeline {
    agent any

    tools {
        maven 'Maven-Local'
    }

    stages {
        stage('Build & Test') {
            steps {
                bat 'mvn clean test'
            }
        }
    }
}
